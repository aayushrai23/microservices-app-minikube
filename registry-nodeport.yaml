apiVersion: v1
kind: Service
metadata:
  name: registry-nodeport
  namespace: kube-system
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30501
  selector:
    kubernetes.io/minikube-addons: registry
